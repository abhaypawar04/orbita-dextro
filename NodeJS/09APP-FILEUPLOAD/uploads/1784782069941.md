import { useState } from "react";

function Todo1() {

&#x20; const \[task, setTask] = useState(\[]);

&#x20; return (

&#x20;   <>

&#x20;     <h1>this is todo learn</h1>

&#x20;     <hr />

&#x20;     <button onClick={() => setTask(\[...task, "task1"])}>add task 1</button>

&#x20;     <button onClick={() => setTask(\[...task, "task2"])}>add task 2</button>

&#x20;     <button onClick={() => setTask(\[...task, "task3"])}>add task 3</button>

&#x20;     <button onClick={() => setTask(\[])}>reset</button>

&#x20;     <hr />

&#x20;     <h2>tasks :</h2>

&#x20;     <ul>

&#x20;       {task.map((item, index) => (

&#x20;         <li>

&#x20;           {item}{" "}

&#x20;           <button onClick={() => setTask(task.filter((\_, i) => i != index))}>

&#x20;             delete

&#x20;           </button>

&#x20;         </li>

&#x20;       ))}

&#x20;     </ul>

&#x20;   </>

&#x20; );

}



export default Todo1;

\-----------------------------------------------------------------------------------------------------

import { useState } from "react";

function Todo1() {

&#x20; const \[task, setTask] = useState(\[]);

&#x20; const \[status, setStatus] = useState("not done");

&#x20; return (

&#x20;   <>

&#x20;     <h1>this is todo learn</h1>

&#x20;     <hr />

&#x20;     <button onClick={() => setTask(\[...task, "task1"])}>add task 1</button>

&#x20;     <button onClick={() => setTask(\[...task, "task2"])}>add task 2</button>

&#x20;     <button onClick={() => setTask(\[...task, "task3"])}>add task 3</button>

&#x20;     <button onClick={() => setTask(\[])}>reset</button>

&#x20;     <hr />

&#x20;     <h2>tasks :</h2>

&#x20;     <ul>

&#x20;       {task.map((item, index) => (

&#x20;         <li>

&#x20;           {item}{" "}

&#x20;           <button onClick={() => setTask(task.filter((\_, i) => i != index))}>

&#x20;             delete

&#x20;           </button>

&#x20;           <button

&#x20;             onClick={() =>

&#x20;               setStatus(status == "not done" ? "done" : "not done")

&#x20;             }

&#x20;           >

&#x20;             {status}

&#x20;           </button>

&#x20;         </li>

&#x20;       ))}

&#x20;     </ul>

&#x20;   </>

&#x20; );

}



export default Todo1;

\------------------------------------------------------------------------------------------

import { useState } from "react";

function Todo1() {

&#x20; const \[task, setTask] = useState(\[]);

&#x20; const \[newTask, setNewTask] = useState(""); //this line added

&#x20; const \[status, setStatus] = useState("not done");

&#x20; return (

&#x20;   <>

&#x20;     <h1>this is todo learn</h1>

&#x20;     <hr />

&#x20;     <button onClick={() => setTask(\[...task, "task1"])}>add task 1</button>

&#x20;     <button onClick={() => setTask(\[...task, "task2"])}>add task 2</button>

&#x20;     <button onClick={() => setTask(\[...task, "task3"])}>add task 3</button>

&#x20;     <button onClick={() => setTask(\[])}>reset</button>



&#x20;     <h1>add more customize task from here: </h1>

&#x20;     <input // input field added

&#x20;       type="text"

&#x20;       placeholder="write task here"

&#x20;       value={newTask} // value here

&#x20;       name="task"

&#x20;       onChange={(e) => setNewTask(e.target.value)} // onchange needed

&#x20;     />

&#x20;     <button onClick={() => setTask(\[...task, newTask])}>add task</button>

&#x20;     <hr />

&#x20;     <h2>tasks :</h2>

&#x20;     <ul>

&#x20;       {task.map((item, index) => (

&#x20;         <li>

&#x20;           {item}{" "}

&#x20;           <button onClick={() => setTask(task.filter((\_, i) => i != index))}>

&#x20;             delete

&#x20;           </button>

&#x20;           <button

&#x20;             onClick={() =>

&#x20;               setStatus(status == "not done" ? "done" : "not done")

&#x20;             }

&#x20;           >

&#x20;             {status}

&#x20;           </button>

&#x20;         </li>

&#x20;       ))}

&#x20;     </ul>

&#x20;   </>

&#x20; );

}



export default Todo1;

\-----------------------------------------------------------------------------------------------

import { useState } from "react";



function Todo1() {

&#x20; const \[task, setTask] = useState(\[]);

&#x20; const \[newTask, setNewTask] = useState("");

&#x20; return (

&#x20;   <>

&#x20;     <h1>here is a todo app</h1>

&#x20;     <hr />

&#x20;     <h2>enter task here:</h2>

&#x20;     <input

&#x20;       type="text"

&#x20;       name="newTask"

&#x20;       value={newTask}

&#x20;       placeholder="enter task here"

&#x20;       onChange={(e) => setNewTask(e.target.value)}

&#x20;     />

&#x20;     <button onClick={() => setTask(\[...task, newTask])}>add task</button>

&#x20;     <hr />

&#x20;     <ul>

&#x20;       {task.map((item, index) => (

&#x20;         <li key={index}>

&#x20;           {item}{" "}

&#x20;           <button onClick={() => setTask(task.filter((\_, i) => i !== index))}>

&#x20;             delete

&#x20;           </button>

&#x20;         </li>

&#x20;       ))}

&#x20;     </ul>

&#x20;   </>

&#x20; );

}

export default Todo1;

\-----------------------------------------------------------------------------------------------------

import { useState } from "react";



function Todo2() {

&#x20; const \[task, setTask] = useState(\[]);

&#x20; const \[newTask, setNewTask] = useState("");

&#x20; return (

&#x20;   <>

&#x20;     <h1>here is a todo app with complete mark</h1>

&#x20;     <hr />

&#x20;     <h2>add new task here :</h2>

&#x20;     <input

&#x20;       type="text"

&#x20;       value={newTask}

&#x20;       name="newTask"

&#x20;       placeholder="add task here"

&#x20;       onChange={(e) => setNewTask(e.target.value)}

&#x20;     />

&#x20;     <button onClick={() => setTask(\[...task, newTask])}>add task</button>

&#x20;     <hr />



&#x20;     <h3>task are here :</h3>

&#x20;     <ul>

&#x20;       {task.map((item, index) => (

&#x20;         <li>

&#x20;           {item}{" "}

&#x20;           <button onClick={() => setTask(task.filter((\_, i) => i != index))}>

&#x20;             delete

&#x20;           </button>

&#x20;         </li>

&#x20;       ))}

&#x20;     </ul>

&#x20;   </>

&#x20; );

}



export default Todo2;

\--------------------------------------------------------------------------------

import { useState } from "react";



function Todo2() {

&#x20; const \[task, setTask] = useState(\[]);

&#x20; const \[newTask, setNewTask] = useState("");

&#x20; return (

&#x20;   <>

&#x20;     <h1>here is todo app with a complete</h1>

&#x20;     <hr />

&#x20;     <input

&#x20;       type="text"

&#x20;       name="newTask"

&#x20;       placeholder="enter task here "

&#x20;       value={newTask}

&#x20;       onChange={(e) => setNewTask(e.target.value)}

&#x20;     />

&#x20;     <button

&#x20;       onClick={() => setTask(\[...task, { text: newTask, completed: false }])}

&#x20;     >

&#x20;       add task

&#x20;     </button>

&#x20;     <hr />

&#x20;     <h1>

&#x20;       <ul>

&#x20;         {task.map((item, index) => (

&#x20;           <li>

&#x20;             <span

&#x20;               style={{ color: item.completed == false ? "red" : "green" }}

&#x20;             >

&#x20;               {item.text}

&#x20;               <button

&#x20;                 onClick={() => setTask(task.filter((\_, i) => i !== index))}

&#x20;               >

&#x20;                 delete

&#x20;               </button>



&#x20;               <button

&#x20;                 onClick={() =>

&#x20;                   setTask(

&#x20;                     task.map((t, i) =>

&#x20;                       i === index ? { ...t, completed: !t.completed } : t,

&#x20;                     ),

&#x20;                   )

&#x20;                 }

&#x20;               >

&#x20;                 {item.completed ? "undo" : "mark as done"}

&#x20;               </button>

&#x20;             </span>

&#x20;           </li>

&#x20;         ))}

&#x20;       </ul>

&#x20;     </h1>

&#x20;   </>

&#x20; );

}



export default Todo2;

\----------------------------------------------------------------------------------------------------------------------------------

import { useState } from "react";



function Todo2() {

&#x20; const \[task, setTask] = useState(\[]);

&#x20; const \[newTask, setNewTask] = useState("");

&#x20; return (

&#x20;   <>

&#x20;     <h1>todo app</h1>

&#x20;     <hr />

&#x20;     <input

&#x20;       type="text"

&#x20;       name="newTask"

&#x20;       value={newTask}

&#x20;       placeholder="write here"

&#x20;       onChange={(e) => setNewTask(e.target.value)}

&#x20;     />

&#x20;     <button

&#x20;       onClick={() => setTask(\[...task, { text: newTask, completed: false }])}

&#x20;     >

&#x20;       add task

&#x20;     </button>

&#x20;     <hr />



&#x20;     <ul>

&#x20;       {task.map((item, index) => (

&#x20;         <li key={index}>

&#x20;           <span style={{ color: item.completed == true ? "green" : "red" }}>

&#x20;             {item.text}

&#x20;           </span>

&#x20;           <button onClick={() => setTask(task.filter((\_, i) => i != index))}>

&#x20;             delete

&#x20;           </button>



&#x20;           <button

&#x20;             onClick={() =>

&#x20;               setTask(

&#x20;                 task.map((t, i) =>

&#x20;                   i === index ? { ...t, completed: !t.completed } : t,

&#x20;                 ),

&#x20;               )

&#x20;             }

&#x20;           >

&#x20;             {item.completed ? "undo" : "mark as done"}

&#x20;           </button>

&#x20;         </li>

&#x20;       ))}

&#x20;     </ul>

&#x20;   </>

&#x20; );

}



export default Todo2;

\--------------------------------------------------------------------------------------------------------------------------------------

\---------------------------------------------------------------------------------------------------------------------------------------

import { useState } from "react";



function Crud() {

&#x20; const \[post, setPost] = useState(\[]);

&#x20; const \[newPost, setNewPost] = useState("");



&#x20; return (

&#x20;   <>

&#x20;     <h1>BLOGIFY</h1>

&#x20;     <hr />

&#x20;     <input

&#x20;       type="text"

&#x20;       name="newPost"

&#x20;       value={newPost}

&#x20;       placeholder="write your blog here"

&#x20;       onChange={(e) => setNewPost(e.target.value)}

&#x20;     />

&#x20;     <button onClick={() => setPost(\[...post, newPost])}>add post </button>

&#x20;     <hr />



&#x20;     <table>

&#x20;       {post.map((item, index) => (

&#x20;         <>

&#x20;           <tr>

&#x20;             <td>{item}</td>

&#x20;             <td>

&#x20;               <button

&#x20;                 onClick={() => setPost(post.filter((\_, i) => i != index))}

&#x20;               >

&#x20;                 delete

&#x20;               </button>

&#x20;             </td>

&#x20;             <td>

&#x20;               <button>edit</button>

&#x20;             </td>

&#x20;           </tr>

&#x20;         </>

&#x20;       ))}

&#x20;     </table>

&#x20;   </>

&#x20; );

}



export default Crud;

\---------------------------------------------------------------------

import { useState } from "react";



function Crud() {

&#x20; const \[post, setPost] = useState(\[]);

&#x20; const \[newPost, setNewPost] = useState("");

&#x20; const \[editIndex, setEditIndex] = useState(null);

&#x20; return (

&#x20;   <>

&#x20;     <h1>BLOGIFY</h1>

&#x20;     <hr />

&#x20;     <input

&#x20;       type="text"

&#x20;       name="newPost"

&#x20;       value={newPost}

&#x20;       placeholder="write your blog here"

&#x20;       onChange={(e) => setNewPost(e.target.value)}

&#x20;     />

&#x20;     {/\* <button onClick={() => setPost(\[...post, newPost])}>

&#x20;       {editIndex !== null ? "update" : "add"}{" "}

&#x20;     </button> \*/}



&#x20;     <button

&#x20;       onClick={() => {

&#x20;         if (editIndex !== null) {

&#x20;           const updatedPosts = \[...post];

&#x20;           updatedPosts\[editIndex] = newPost;

&#x20;           setPost(updatedPosts);

&#x20;           setEditIndex(null);

&#x20;         } else {

&#x20;           setPost(\[...post, newPost]);

&#x20;         }

&#x20;         setNewPost("");

&#x20;       }}

&#x20;     >

&#x20;       {editIndex !== null ? "update" : "add"}

&#x20;     </button>



&#x20;     <hr />



&#x20;     <table>

&#x20;       {post.map((item, index) => (

&#x20;         <>

&#x20;           <tr>

&#x20;             <td>{item}</td>

&#x20;             <td>

&#x20;               <button

&#x20;                 onClick={() => setPost(post.filter((\_, i) => i != index))}

&#x20;               >

&#x20;                 delete

&#x20;               </button>

&#x20;             </td>

&#x20;             <td>

&#x20;               <button

&#x20;                 onClick={() => {

&#x20;                   setNewPost(item);

&#x20;                   setEditIndex(index);

&#x20;                 }}

&#x20;               >

&#x20;                 edit

&#x20;               </button>

&#x20;             </td>

&#x20;           </tr>

&#x20;         </>

&#x20;       ))}

&#x20;     </table>

&#x20;   </>

&#x20; );

}



export default Crud;

\-- ----------------------------------------------------------------------------------------

import { useState } from "react";



function CrudD2() {

&#x20; const \[post, setPost] = useState(\[]);

&#x20; const \[newPost, setNewPost] = useState("");

&#x20; const \[updateIndex, setUpdateIndex] = useState(null);



&#x20; return (

&#x20;   <>

&#x20;     <h1>Postify</h1>

&#x20;     <hr />



&#x20;     <div>

&#x20;       <input

&#x20;         type="text"

&#x20;         name="newPost"

&#x20;         value={newPost}

&#x20;         placeholder="enter post here"

&#x20;         onChange={(e) => setNewPost(e.target.value)}

&#x20;       />

&#x20;       <button

&#x20;         onClick={() => {

&#x20;           if (updateIndex !== null) {

&#x20;             const updatedPosts = \[...post];

&#x20;             updatedPosts\[updateIndex] = newPost;

&#x20;             setPost(updatedPosts);

&#x20;             setUpdateIndex(null);

&#x20;           } else {

&#x20;             setPost(\[...post, newPost]);

&#x20;           }

&#x20;           setNewPost("");

&#x20;         }}

&#x20;       >

&#x20;         {updateIndex !== null ? "edit" : "add"}

&#x20;       </button>

&#x20;     </div>



&#x20;     <div>

&#x20;       <table>

&#x20;         <tbody>

&#x20;           {post.map((item, index) => (

&#x20;             <tr key={index}>

&#x20;               <th>{item}</th>

&#x20;               <th>

&#x20;                 <button

&#x20;                   onClick={() => {

&#x20;                     setNewPost(item);

&#x20;                     setUpdateIndex(index);

&#x20;                   }}

&#x20;                 >

&#x20;                   edit

&#x20;                 </button>

&#x20;               </th>

&#x20;               <th>

&#x20;                 <button

&#x20;                   onClick={() => setPost(post.filter((\_, i) => i !== index))}

&#x20;                 >

&#x20;                   delete

&#x20;                 </button>

&#x20;               </th>

&#x20;             </tr>

&#x20;           ))}

&#x20;         </tbody>

&#x20;       </table>

&#x20;     </div>

&#x20;   </>

&#x20; );

}



export default CrudD2;



\-- ----------------------------------------------------------------------------------------

import { useState } from "react";



function ExpenseApp() {

&#x20; const \[title, setTitle] = useState("");

&#x20; const \[amount, setAmount] = useState("");

&#x20; const \[category, setCategory] = useState("");



&#x20; const \[expenses, setExpenses] = useState(\[]);

&#x20; const \[editIndex, setEditIndex] = useState(null);



&#x20; const resetForm = () => {

&#x20;   setTitle("");

&#x20;   setAmount("");

&#x20;   setCategory("");

&#x20; };



&#x20; const handleExpense = () => {

&#x20;   if (!title.trim() || !amount || !category.trim()) {

&#x20;     alert("Please fill all fields");

&#x20;     return;

&#x20;   }



&#x20;   if (editIndex !== null) {

&#x20;     setExpenses((prev) =>

&#x20;       prev.map((expense, index) =>

&#x20;         index === editIndex

&#x20;           ? {

&#x20;               ...expense,

&#x20;               title,

&#x20;               amount: Number(amount),

&#x20;               category,

&#x20;             }

&#x20;           : expense,

&#x20;       ),

&#x20;     );



&#x20;     setEditIndex(null);

&#x20;   } else {

&#x20;     const newExpense = {

&#x20;       id: Date.now(),

&#x20;       title,

&#x20;       amount: Number(amount),

&#x20;       category,

&#x20;     };



&#x20;     setExpenses((prev) => \[...prev, newExpense]);

&#x20;   }



&#x20;   resetForm();

&#x20; };



&#x20; const handleDelete = (index) => {

&#x20;   setExpenses((prev) => prev.filter((\_, i) => i !== index));



&#x20;   if (editIndex === index) {

&#x20;     setEditIndex(null);

&#x20;     resetForm();

&#x20;   }

&#x20; };



&#x20; const handleEdit = (expense, index) => {

&#x20;   setTitle(expense.title);

&#x20;   setAmount(expense.amount);

&#x20;   setCategory(expense.category);

&#x20;   setEditIndex(index);

&#x20; };



&#x20; return (

&#x20;   <>

&#x20;     <h1>Expensio</h1>

&#x20;     <hr />



&#x20;     <div>

&#x20;       <input

&#x20;         type="text"

&#x20;         value={title}

&#x20;         placeholder="Enter title"

&#x20;         onChange={(e) => setTitle(e.target.value)}

&#x20;       />



&#x20;       <input

&#x20;         type="number"

&#x20;         value={amount}

&#x20;         placeholder="Enter amount"

&#x20;         onChange={(e) => setAmount(e.target.value)}

&#x20;       />



&#x20;       <input

&#x20;         type="text"

&#x20;         value={category}

&#x20;         placeholder="Enter category"

&#x20;         onChange={(e) => setCategory(e.target.value)}

&#x20;       />



&#x20;       <button onClick={handleExpense}>

&#x20;         {editIndex !== null ? "Update Expense" : "Add Expense"}

&#x20;       </button>

&#x20;     </div>



&#x20;     <hr />



&#x20;     <table border="1" cellPadding="10">

&#x20;       <thead>

&#x20;         <tr>

&#x20;           <th>Title</th>

&#x20;           <th>Amount</th>

&#x20;           <th>Category</th>

&#x20;           <th colSpan="2">Actions</th>

&#x20;         </tr>

&#x20;       </thead>



&#x20;       <tbody>

&#x20;         {expenses.map((expense, index) => (

&#x20;           <tr key={expense.id}>

&#x20;             <td>{expense.title}</td>

&#x20;             <td>{expense.amount}</td>

&#x20;             <td>{expense.category}</td>



&#x20;             <td>

&#x20;               <button onClick={() => handleEdit(expense, index)}>Edit</button>

&#x20;             </td>



&#x20;             <td>

&#x20;               <button onClick={() => handleDelete(index)}>Delete</button>

&#x20;             </td>

&#x20;           </tr>

&#x20;         ))}

&#x20;       </tbody>

&#x20;     </table>

&#x20;   </>

&#x20; );

}



export default ExpenseApp;

\-------------------------------------------------------------------------------------------

import { useState } from "react";



function ExpenceApp() {

&#x20; const \[title, setTitle] = useState("");

&#x20; const \[amount, setAmount] = useState("");

&#x20; const \[category, setCategory] = useState("");



&#x20; const \[expenses, setExpenses] = useState(\[]);

&#x20; const \[editIndex, setEditIndex] = useState(null);

&#x20; //---

&#x20; //---

&#x20; const handleEdit = (item, index) => {

&#x20;   setTitle(item.title);

&#x20;   setAmount(item.amount);

&#x20;   setCategory(item.category);

&#x20;   setEditIndex(index);

&#x20; };

&#x20; //----

&#x20; //----

&#x20; const handleDelete = (index) => {

&#x20;   setExpenses(() => expenses.filter((\_, i) => i !== index));

&#x20; };

&#x20; //---

&#x20; //---

&#x20; //   const handleAdd = () => {

&#x20; //     if (editIndex !== null) {

&#x20; //     } else {

&#x20; //       const newExpenses = {

&#x20; //         id: Date.now(),

&#x20; //         title,

&#x20; //         amount: Number(amount),

&#x20; //         category,

&#x20; //       };

&#x20; //       setExpenses((prev) => \[...prev, newExpenses]);

&#x20; //     }

&#x20; //   };



&#x20; //-----------------------------------------

&#x20; const handleAdd = (item, index) => {

&#x20;   if (editIndex !== null) {

&#x20;     setExpenses((prev) =>

&#x20;       prev.map((item, index) =>

&#x20;         index == editIndex

&#x20;           ? {

&#x20;               ...item,

&#x20;               title,

&#x20;               amount: Number(amount),

&#x20;               category,

&#x20;             }

&#x20;           : item,

&#x20;       ),

&#x20;     );

&#x20;     setEditIndex(null);

&#x20;   } else {

&#x20;     const newExpenses = {

&#x20;       id: Date.now(),

&#x20;       title,

&#x20;       amount: Number(amount),

&#x20;       category,

&#x20;     };



&#x20;     setExpenses((prev) => \[...prev, newExpenses]);

&#x20;   }

&#x20; };

&#x20; //-----------------------------------------



&#x20; //---

&#x20; //---

&#x20; return (

&#x20;   <>

&#x20;     <h1>Expensio</h1> <hr />

&#x20;     <div>

&#x20;       <table>

&#x20;         <tbody>

&#x20;           <tr>

&#x20;             <th>

&#x20;               <input

&#x20;                 type="text"

&#x20;                 name="title"

&#x20;                 value={title}

&#x20;                 placeholder="enter title"

&#x20;                 onChange={(e) => setTitle(e.target.value)}

&#x20;               />

&#x20;             </th>

&#x20;             <th>

&#x20;               {" "}

&#x20;               <input

&#x20;                 type="text"

&#x20;                 name="amount"

&#x20;                 value={amount}

&#x20;                 placeholder="enter amount"

&#x20;                 onChange={(e) => setAmount(e.target.value)}

&#x20;               />

&#x20;             </th>

&#x20;             <th>

&#x20;               {" "}

&#x20;               <input

&#x20;                 type="text"

&#x20;                 name="category"

&#x20;                 value={category}

&#x20;                 placeholder="enter category"

&#x20;                 onChange={(e) => setCategory(e.target.value)}

&#x20;               />

&#x20;             </th>

&#x20;             <th>

&#x20;               <button onClick={() => handleAdd()}>ADD Expences</button>

&#x20;             </th>

&#x20;           </tr>

&#x20;         </tbody>

&#x20;       </table>

&#x20;     </div>

&#x20;     {/\* ----------------------------------------------------------- \*/}

&#x20;     <table>

&#x20;       <tbody>

&#x20;         <tr>

&#x20;           <th>title</th>

&#x20;           <th>amount</th>

&#x20;           <th>category</th>

&#x20;         </tr>

&#x20;         {expenses.map((item, index) => (

&#x20;           <tr>

&#x20;             <td>{item.title}</td>

&#x20;             <td>{item.amount}</td>

&#x20;             <td>{item.category}</td>

&#x20;             <td>

&#x20;               <button onClick={() => handleDelete(index)}>delete</button>

&#x20;             </td>

&#x20;             <td>

&#x20;               <button onClick={() => handleEdit(item, index)}>edit</button>

&#x20;             </td>

&#x20;           </tr>

&#x20;         ))}

&#x20;       </tbody>

&#x20;     </table>

&#x20;   </>

&#x20; );

}



export default ExpenceApp;

\------------------------------------------------------------------------------------

import { useState } from "react";



function ExpenceApp() {

&#x20; const \[title, setTitle] = useState("");

&#x20; const \[amount, setAmount] = useState("");

&#x20; const \[category, setCategory] = useState("");



&#x20; const \[expenses, setExpenses] = useState(\[]);

&#x20; const \[editIndex, setEditIndex] = useState(null);

&#x20; //---

&#x20; //---

&#x20; const handleEdit = (item, index) => {

&#x20;   setTitle(item.title);

&#x20;   setAmount(item.amount);

&#x20;   setCategory(item.category);

&#x20;   setEditIndex(index);

&#x20; };

&#x20; //----

&#x20; //----

&#x20; const handleDelete = (index) => {

&#x20;   setExpenses(() => expenses.filter((\_, i) => i !== index));

&#x20; };

&#x20; //---

&#x20; //-----------------------------------------

&#x20; const handleAdd = () => {

&#x20;   if (editIndex !== null) {

&#x20;     setExpenses((prev) =>

&#x20;       prev.map((item, index) =>

&#x20;         index == editIndex

&#x20;           ? {

&#x20;               ...item,

&#x20;               title,

&#x20;               amount: Number(amount),

&#x20;               category,

&#x20;             }

&#x20;           : item,

&#x20;       ),

&#x20;     );

&#x20;     setEditIndex(null);

&#x20;   } else {

&#x20;     const newExpenses = {

&#x20;       id: Date.now(),

&#x20;       title,

&#x20;       amount: Number(amount),

&#x20;       category,

&#x20;     };



&#x20;     setExpenses((prev) => \[...prev, newExpenses]);

&#x20;   }

&#x20;   setTitle("");

&#x20;   setAmount("");

&#x20;   setCategory("");

&#x20; };

&#x20; //-----------------------------------------



&#x20; //---

&#x20; const total = expenses.reduce((sum, expense) => {

&#x20;   return sum + expense.amount;

&#x20; }, 0);

&#x20; //---

&#x20; return (

&#x20;   <>

&#x20;     <h1>Expensio</h1> <hr />

&#x20;     <div>

&#x20;       <table>

&#x20;         <tbody>

&#x20;           <tr>

&#x20;             <th>

&#x20;               <input

&#x20;                 type="text"

&#x20;                 name="title"

&#x20;                 value={title}

&#x20;                 placeholder="enter title"

&#x20;                 onChange={(e) => setTitle(e.target.value)}

&#x20;               />

&#x20;             </th>

&#x20;             <th>

&#x20;               {" "}

&#x20;               <input

&#x20;                 type="text"

&#x20;                 name="amount"

&#x20;                 value={amount}

&#x20;                 placeholder="enter amount"

&#x20;                 onChange={(e) => setAmount(e.target.value)}

&#x20;               />

&#x20;             </th>

&#x20;             <th>

&#x20;               {" "}

&#x20;               <input

&#x20;                 type="text"

&#x20;                 name="category"

&#x20;                 value={category}

&#x20;                 placeholder="enter category"

&#x20;                 onChange={(e) => setCategory(e.target.value)}

&#x20;               />

&#x20;             </th>

&#x20;             <th>

&#x20;               <button onClick={() => handleAdd()}>

&#x20;                 {editIndex !== null ? "Update Expense" : "Add Expense"}

&#x20;               </button>

&#x20;             </th>

&#x20;           </tr>

&#x20;         </tbody>

&#x20;       </table>

&#x20;     </div>

&#x20;     {/\* ----------------------------------------------------------- \*/}

&#x20;     <table>

&#x20;       <tbody>

&#x20;         <tr>

&#x20;           <th>title</th>

&#x20;           <th>amount</th>

&#x20;           <th>category</th>

&#x20;         </tr>

&#x20;         {expenses.map((item, index) => (

&#x20;           <tr key={item.id}>

&#x20;             <td>{item.title}</td>

&#x20;             <td>{item.amount}</td>

&#x20;             <td>{item.category}</td>

&#x20;             <td>

&#x20;               <button onClick={() => handleDelete(index)}>delete</button>

&#x20;             </td>

&#x20;             <td>

&#x20;               <button onClick={() => handleEdit(item, index)}>edit</button>

&#x20;             </td>

&#x20;           </tr>

&#x20;         ))}

&#x20;       </tbody>

&#x20;     </table>

&#x20;     <h2>Total Expenses: ₹{total}</h2>

&#x20;   </>

&#x20; );

}



export default ExpenceApp;

\---------------------------------------------------------------------------------------

\-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

import { useState } from "react";



function Weather() {

&#x20; const \[city, setCity] = useState("");

&#x20; const \[weather, setWeather] = useState(null);



&#x20; const fetchWeather = async () => {

&#x20;   const res = await fetch(

&#x20;     `https://api.openweathermap.org/data/2.5/weather?q=${city}\&appid=c85f7ce022f3219cd6024aeae9f486c3\&units=metric`,

&#x20;   );



&#x20;   const data = await res.json();



&#x20;   setWeather(data);

&#x20; };



&#x20; return (

&#x20;   <>

&#x20;     <h1>Atmospherio</h1>

&#x20;     <hr />



&#x20;     <input

&#x20;       type="text"

&#x20;       value={city}

&#x20;       placeholder="Enter city"

&#x20;       onChange={(e) => setCity(e.target.value)}

&#x20;     />



&#x20;     <button onClick={fetchWeather}>Search</button>



&#x20;     {weather \&\& (

&#x20;       <div>

&#x20;         <h2>{weather.name}</h2>

&#x20;         <p>Temperature: {weather.main.temp}°C</p>

&#x20;         <p>Condition: {weather.weather\[0].main}</p>

&#x20;       </div>

&#x20;     )}

&#x20;   </>

&#x20; );

}



export default Weather;



\--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

import { useEffect, useState } from "react";



function Weather() {

&#x20; const \[city, setCity] = useState("pune");

&#x20; const \[weather, setWeather] = useState("");

&#x20; //-----

&#x20; ***const fetchWeather = async () => {***

&#x20;   ***const res = await fetch(***

&#x20;     ***`https://api.openweathermap.org/data/2.5/weather?q=${city}\&appid=c85f7ce022f3219cd6024aeae9f486c3\&units=metric`,***

&#x20;   ***);***

&#x20;   ***const data = await res.json();***

&#x20;   ***setWeather(data);***

&#x20; ***};***

&#x20; //-----

&#x20; ***useEffect(() => {***

&#x20;   ***fetchWeather();***

&#x20; ***}, \[]);***

&#x20; return (

&#x20;   <>

&#x20;     <h1>Weather</h1> <hr />

&#x20;     <input

&#x20;       type="text"

&#x20;       name="city"

&#x20;       value={city}

&#x20;       placeholder="enter city to check weather"

&#x20;       onChange={(e) => setCity(e.target.value)}

&#x20;     />

&#x20;     <button onClick={() => fetchWeather()}>check weather</button>

&#x20;     <div>

&#x20;       {weather \&\& (

&#x20;         <div>

&#x20;           <h2>{weather.name}</h2>

&#x20;           <p>Temperature: {weather.main.temp}°C</p>

&#x20;           <p>Condition: {weather.weather\[0].main}</p>

&#x20;         </div>

&#x20;       )}

&#x20;     </div>

&#x20;   </>

&#x20; );

}

export default Weather;

\----------------------------------------------------------------------------------------------

import { useEffect, useState } from "react";



function Clock() {

&#x20; const \[time, setTime] = useState(new Date());

&#x20; useEffect(() => {

&#x20;   const id = setInterval(() => {

&#x20;     setTime(new Date());

&#x20;   }, 1000);

&#x20;   return () => clearInterval(id);

&#x20; }, \[]);



&#x20; return (

&#x20;   <>

&#x20;     <h1>Clock</h1>

&#x20;     <p> time: {time.toLocaleTimeString()}</p>

&#x20;   </>

&#x20; );

}



export default Clock;

\----------------------------------------------------------------------------------------------

\-------------------------------------------------------------------------------------------

\----------------------------------------------------------------------------------------------

import { useRef } from "react";



function AutoFocus() {

&#x20; const inputRef = useRef();

&#x20; const handleFocus = () => {

&#x20;   inputRef.current.focus();

&#x20; };

&#x20; return (

&#x20;   <>

&#x20;     <h1>AutoFocus</h1>

&#x20;     <hr />

&#x20;     <input type="text" ref={inputRef} />

&#x20;     <button onClick={() => handleFocus()}> focus</button>

&#x20;   </>

&#x20; );

}



export default AutoFocus;

\--------------------------------------------------------------------------------------------

import { useEffect, useState } from "react";

function Pr5Clock() {
  const [time, setTime] = useState(new Date());
  useEffect(() => {
    const t1 = setInterval(() => {
      setTime(new Date());
    }, 1000);

    return () => clearInterval(t1);
  }, []);
  return (
    <>
      <h1>clock</h1>
      <hr />
      <h1>{time.toLocaleTimeString()}</h1>
    </>
  );
}
export default Pr5Clock;
--- ------------------------------------------------------------------------------------------
import { useEffect, useRef, useState } from "react";

function StopWatch() {
  const [time, setTime] = useState(0);
  const [isRunning, setIsRunning] = useState(false);

  const intervalRef = useRef(null);

  useEffect(() => {
    if (isRunning) {
      intervalRef.current = setInterval(() => {
        setTime((prev) => prev + 1);
      }, 1000);
    } else {
      clearInterval(intervalRef.current);
    }

    return () => clearInterval(intervalRef.current);
  }, [isRunning]);

  return (
    <>
      <h1>stopwatch</h1>
      <hr />

      <div>
        <h2>{time} sec</h2>

        <button onClick={() => setIsRunning(true)}>start</button>
        <button onClick={() => setIsRunning(false)}>Stop</button>
        <button
          onClick={() => {
            setIsRunning(false);
            setTime(0);
          }}
        >
          reset
        </button>
      </div>
    </>
  );
}

export default StopWatch;
-- ----------------------------------------------------------------------
import { useEffect, useRef, useState } from "react";

function StopWatch() {
  const [time, setTime] = useState(0);
  const [isRunning, setIsRunning] = useState(false);

  const t1 = useRef(null);

  useEffect(() => {
    if (isRunning) {
      t1.current = setInterval(() => {
        setTime((prev) => prev + 1);
      }, 1000);
    } else {
      clearInterval(t1.current);
    }
    return () => clearInterval(t1.current);
  }, [isRunning]);
  return (
    <>
      <h1>stopwatch</h1>
      <hr />
      <h1>{time} sec</h1>
      <button onClick={() => setIsRunning(true)}>start</button>
      <button onClick={() => setIsRunning(false)}>stop</button>
      <button
        onClick={() => {
          setIsRunning(false);
          setTime(0);
        }}
      >
        reset
      </button>
    </>
  );
}

export default StopWatch;

// learn interval // clear interval // useref //
-- ------------------------------------------------------------------------------
import { useEffect, useRef, useState } from "react";

function CharCount() {
  const [text, setText] = useState("");
  const previousCount = useRef(0);
  const currentCount = text.length;

  useEffect(() => {
    previousCount.current = currentCount;
  }, [currentCount]);
  return (
    <>
      <h1>charcounter</h1> <hr />
      <input
        type="text"
        placeholder="Type something..."
        value={text}
        onChange={(e) => setText(e.target.value)}
      />
      <h3>Current Count: {currentCount}</h3>
      <h3>Previous Count: {previousCount.current}</h3>
    </>
  );
}
export default CharCount;
----------------
counter app
todo list with crud 
expense tracker 
weather search app
live clock 
auto focus input 
stopwatch 
character counter 
product search 
expense analytics 
parent child optimization 
search component 
theme switcher 
user authentication 
shopping cart
multi step form 
-------------------------------------------------------------------------------------
import { useState } from "react";
import products from "./produsts";

function ProductSearch() {
  const [search, setSearch] = useState("");
  const [category, setCategory] = useState("All");
  

  // Runs on EVERY render ------------------------------------------------
  const filteredProducts = products.filter((product) => {
    const searchMatch = product.name
      .toLowerCase()
      .includes(search.toLowerCase());

    const categoryMatch = category === "All" || product.category === category;

    return searchMatch && categoryMatch;
  });
  //----------------------------------------------------------------------
  return (
    <div style={{ padding: "20px" }}>
      <h1>Product Search (Without useMemo)</h1>

      {/* Search */}
      <input
        type="text"
        placeholder="Search Product"
        value={search}
        onChange={(e) => setSearch(e.target.value)}
      />
      {/* ---------------------------------------------------------------------------- */}
      {/* Category */}
      <select
        value={category}
        onChange={(e) => setCategory(e.target.value)}
        style={{ marginLeft: "10px" }}
      >
        <option value="All">All</option>
        <option value="Electronics">Electronics</option>
        <option value="Clothing">Clothing</option>
      </select>
      {/* ---------------------------------------------------------------------------- */}
      <hr />
      <h3>Products Found: {filteredProducts.length}</h3>
      {/* ---------------------------------------------------------------------------- */}

      <ul>
        {filteredProducts.slice(0, 20).map((product) => (
          <li key={product.id}>
            {product.name} - {product.category}
          </li>
        ))}
      </ul>
    </div>
  );
}

export default ProductSearch;
-------------------------------------------------------------------------------------------
import { useState } from "react";
import products from "./produsts";

function ProductSearch() {
  const [search, setSearch] = useState("");
  const [category, setCategory] = useState("All");

  const filteredProducts = products.filter((product) => {
    const searchMatch = product.name
      .toLowerCase()
      .includes(search.toLowerCase());
    const categoryMatch = category === "All" || product.category === category;
    return searchMatch && categoryMatch;
  });

  return (
    <>
      <h1>product Search</h1>
      <input
        type="text"
        placeholder="search product"
        value={search}
        onChange={(e) => setSearch(e.target.value)}
      />

      <select
        name="category"
        value={category}
        onChange={(e) => setCategory(e.target.value)}
      >
        <option value="All">All</option>
        <option value="Electronics">Electronics</option>
        <option value="Clothing">Clothing</option>
      </select>

      <div>
        <h3>product founds : {filteredProducts.length}</h3>

        <ul>
          {filteredProducts.map((product) => (
            <li key={product.id}>
              {product.name} , {product.category}
            </li>
          ))}
        </ul>
      </div>
    </>
  );
}

export default ProductSearch;
-- ----------------------------------------------------------------------------------------

import { useState } from "react";
import products from "./produsts";

function ProductSearch() {
  const [search, setSearch] = useState("");
  const [category, setCategory] = useState("All");

  const filteredProducts = products.filter((product) => {
    const searchMatch = product.name
      .toLowerCase()
      .includes(search.toLowerCase());
    const categoryMatch = category === "All" || product.category === category;
    return searchMatch && categoryMatch;
  });

  return (
    <>
      <h1>product Search</h1>
      <input
        type="text"
        placeholder="search product"
        value={search}
        onChange={(e) => setSearch(e.target.value)}
      />

      <select
        name="category"
        value={category}
        onChange={(e) => setCategory(e.target.value)}
      >
        <option value="All">All</option>
        <option value="Electronics">Electronics</option>
        <option value="Clothing">Clothing</option>
      </select>

      <div>
        <h3>product founds : {filteredProducts.length}</h3>

        <ul>
          {filteredProducts.map((product) => (
            <li key={product.id}>
              {product.name} , {product.category}
            </li>
          ))}
        </ul>
      </div>
    </>
  );
}

export default ProductSearch;
-- -----------------------------------------------------

import { useState } from "react";
import products from "./produsts";

//c

function ProductSearch() {
  const [search, setSearch] = useState("");
  const [category, setCategory] = useState("All");

  //c

  const filteredData = products.filter((product) => {
    const matchSearch = product.name
      .toLowerCase()
      .includes(search.toLowerCase()); //c
    const matchCategory = category === "All" || product.category === category;
    return matchCategory && matchSearch;
  });
  return (
    <>
      <div>
        <h1>product search</h1>

        <input
          type="text"
          name="search"
          value={search}
          placeholder="search here"
          onChange={(e) => setSearch(e.target.value)}
        />

        <select
          name="category"
          value={category}
          onChange={(e) => setCategory(e.target.value)}
        >
          <option value="All">ALL</option>
          <option value="Electronics">ELECTRONICS</option>
          <option value="Clothing">CLOTHING</option>
        </select>
      </div>

      <div>
        <ul>
          {filteredData.map((item, index) => (
            <li key={item.id}>
              {item.name},{item.category}
            </li>
          ))}
        </ul>
      </div>
    </>
  );
}

export default ProductSearch;

--- ------------------------
import { useState } from "react";
import products from "./produsts"; //c

function ProductSearch() {
  const [search, setSearch] = useState("");
  const [category, setCategory] = useState("All"); //c

  const filteredData = products.filter((product) => {
    const matchSearch = product.name
      .toLowerCase()
      .includes(search.toLowerCase());
    const matchCategory = category === "All" || product.category === category;

    return matchCategory && matchSearch;
  });
  return (
    <>
      <h1>product search</h1>
      <div>
        <input
          type="text"
          name="search"
          value={search}
          onChange={(e) => setSearch(e.target.value)}
        />

        <select
          name="category"
          value={category}
          onChange={(e) => setCategory(e.target.value)}
        >
          <option value="All">All</option>
          <option value="Electronics">Electronics</option>
          <option value="Clothing">Clothing</option>
        </select>
      </div>

      <div>
        <ul>
          {filteredData.map((item, index) => (
            <li key={item.id}>
              {item.name} , {item.category}
            </li>
          ))}
        </ul>
      </div>
    </>
  );
}

export default ProductSearch;

-- -------------------------------------------------------------------------------------------------------
import { useMemo, useState } from "react";
import expenses from "./expenses";
//c
function ExpenseAnalytics() {
  const [category, setCategory] = useState("All");
  //c
  // Filter expenses
  const filteredExpenses = expenses.filter(
    (expense) => category === "All" || expense.category === category,
  );
  //c
  // Total expense
  const totalExpense = useMemo(() => {
    console.log("Calculating total...");

    return filteredExpenses.reduce((sum, expense) => sum + expense.amount, 0);
  }, [filteredExpenses]);
  //c
  // Category Breakdown
  const categoryBreakdown = useMemo(() => {
    console.log("Calculating breakdown...");

    return filteredExpenses.reduce((acc, expense) => {
      acc[expense.category] = (acc[expense.category] || 0) + expense.amount;

      return acc;
    }, {});
  }, [filteredExpenses]);

  return (
    <div>
      <h1>Expense Analytics</h1>

      <hr />

      <div>
        <label>Category : </label>

        <select value={category} onChange={(e) => setCategory(e.target.value)}>
          <option value="All">All</option>
          <option value="Food">Food</option>
          <option value="Transport">Transport</option>
          <option value="Entertainment">Entertainment</option>
        </select>
      </div>

      <hr />

      <h2>Total Expense : ₹{totalExpense}</h2>

      <hr />

      <h2>Category Breakdown</h2>

      <ul>
        {Object.entries(categoryBreakdown).map(([category, amount]) => (
          <li key={category}>
            {category} : ₹{amount}
          </li>
        ))}
      </ul>

      <hr />

      <h2>Expense List ({filteredExpenses.length})</h2>

      <ul>
        {filteredExpenses.map((expense) => (
          <li key={expense.id}>
            {expense.title} - ₹{expense.amount} ({expense.category})
          </li>
        ))}
      </ul>
    </div>
  );
}

export default ExpenseAnalytics;
-- --------------------------------------
import { useState } from "react";
import SearchBox from "./components/SearchBox";
import ResultList from "./components/ResultList";

function App() {
  const [search, setSearch] = useState("");
  const products = [
    "Apple",
    "Banana",
    "Orange",
    "Mango",
    "Pineapple",
    "Watermelon",
    "Kiwi",
    "Grapes",
    "Papaya",
    "Strawberry",
  ];
  // filter
  const filteredProducts = products.filter((product) =>
    product.toLowerCase().includes(search.toLowerCase()),
  );
  return (
    <>
      <SearchBox search={search} onSearch={setSearch} />
      <ResultList products={filteredProducts} />
    </>
  );
}
export default App;

---
function SearchBox({ search, onSearch }) {
  return (
    <>
      <input
        type="text"
        name="search"
        value={search}
        onChange={(e) => onSearch(e.target.value)}
        placeholder="search products"
      />
    </>
  );
}
export default SearchBox;
---
function ResultList({ products }) {
  if (products.length === 0 || products.length < 0)
    return <p>no result found</p>;

  return (
    <>
      <ul>
        {products.map((item, index) => (
          <li key={index}>{item}</li>
        ))}
      </ul>
    </>
  );
}
export default ResultList;
------------------------------------------------------------------------
import { createContext, useContext, useState } from "react";
const ThemeContext = createContext();
function App() {
  const [theme, setTheme] = useState("light");

  const toggleTheme = () => {
    setTheme((prev) => (prev === "light" ? "dark" : "light"));
  };
  return (
    <>
      <h1> App is Here</h1>
      <button onClick={toggleTheme}>change theme</button>

      <ThemeContext.Provider value={{ theme, toggleTheme }}>
        <Page />
      </ThemeContext.Provider>
    </>
  );
}

export default App;

export function Page() {
  const { theme } = useContext(ThemeContext);
  return (
    <>
      <h1>Page component</h1>
      <div
        style={{
          height: "100vh",
          background: theme === "light" ? "#c9c3c3" : "#1e1e1e",
          color: theme === "light" ? "#772d2d" : "#f5f5f5",
        }}
      >
        <h1>here is demo</h1>
        <h1>here is demo</h1>
        <h1>here is demo</h1>
        <h1>here is demo</h1>
        <h1>here is demo</h1>
        <h1>here is demo</h1>
      </div>
    </>
  );
}
-----------------------------------------------------------------------------------------
import { createContext, useContext, useState } from "react";

const ThemeContext = createContext();

function App() {
  const [theme, setTheme] = useState("light");
  const toggleTheme = () => {
    setTheme((prev) => (prev === "light" ? "dark" : "light"));
  };
  return (
    <>
      <ThemeContext.Provider value={{ theme, toggleTheme }}>
        <h1>Context</h1>
        <button onClick={toggleTheme}>
          switch to {theme == "light" ? "dark" : "light"}
        </button>
        <Page />
      </ThemeContext.Provider>
    </>
  );
}
export default App;

//------------------------------------------------
export function Page() {
  const { theme } = useContext(ThemeContext);
  return (
    <>
      <div
        style={{
          background: theme === "light" ? "#e2e0e0" : "#000000",
          color: theme === "light" ? "#000000" : "#dddada",
        }}
      >
        <h1>page is here </h1>
        <h1>page is here </h1>
        <h1>page is here </h1>
        <h1>page is here </h1>
        <h1>page is here </h1>
        <h1>page is here </h1>
      </div>
    </>
  );
}
-----------------------------------------------------------------------------------------
AuthContext.jsx
const AuthContext = createContext(); 
--- 
App.jsx
const [user, setUser] 
const login
const logout
provider-> dashboard 
---
dashboard.jsx
user ? logout userinfo : login

-- 
loginbutton.jsx

logoutbutton.jsx

userinfo.jsx

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# 🧭 COMPLETE BACKEND ROADMAP (Node + Express + MongoDB)
---
# 🟢 PHASE 1: JavaScript Backend Basics (Must know first)
## Topics
* What is backend?
* How Node.js works
* Event Loop (basic idea)
* Modules (CommonJS)
* npm basics
* fs, path, os modules (basic)
* HTTP basics (request/response)
## Mini Practice
* Read/write a file using Node (`fs`)
* Simple HTTP server using `http` module
## Mini Project
### 🔹 Project 1: Simple Node Server
* Routes:
  * `/`
  * `/about`
* Send plain text responses
---
# 🟡 PHASE 2: Express.js Fundamentals
## Topics
* What is Express?
* Creating server
* Routing (GET, POST, PUT, DELETE)
* Middleware concept
* Request & Response objects
* JSON handling
* Postman usage
## Mini Project
### 🔹 Project 2: Notes API (In-memory)
* CRUD notes:
  * Create note
  * Get all notes
  * Get single note
  * Delete note
(No database yet — only array)
---
# 🟠 PHASE 3: MongoDB Basics
## Topics
* What is database?
* SQL vs NoSQL
* MongoDB structure (DB → Collection → Document)
* BSON/JSON
* CRUD in MongoDB
* MongoDB Compass
* Mongoose introduction
## Mini Project
### 🔹 Project 3: User Storage API (MongoDB)
* Connect MongoDB
* Create user
* Get users
* Delete user
---
# 🔵 PHASE 4: Mongoose Deep Dive
## Topics
* Schemas
* Models
* Data types
* Validation
* Relationships (basic)
* Timestamps
## Mini Project
### 🔹 Project 4: Student Management System
* Add student
* Update student marks
* Delete student
* Get top students
---
# 🔴 PHASE 5: Authentication System (VERY IMPORTANT)
## Topics
* Password hashing (bcrypt)
* JWT (JSON Web Token)
* Login / Signup flow
* Protected routes
* Middleware auth check
## Mini Project
### 🔹 Project 5: Auth System API
* Signup user
* Login user
* Protected route `/profile`
* Token-based authentication
---
# 🟣 PHASE 6: Advanced Express
## Topics
* Middleware deep dive
* Error handling middleware
* Async/await handling
* API structure (MVC pattern)
* Environment variables (.env)
## Mini Project
### 🔹 Project 6: Blog API (Clean Architecture)
* Create blog post
* Update blog
* Delete blog
* Get all blogs
* User-based blogs
---
# ⚫ PHASE 7: Real World Backend Features
## Topics
* File uploads (Multer)
* Pagination
* Search & filtering
* Rate limiting
* CORS
* Security basics (helmet)
* API versioning
## Mini Project
### 🔹 Project 7: E-commerce Backend
* Products API
* Search products
* Upload product images
* Pagination
---
# 🟤 PHASE 8: Full Stack Integration
## Topics
* Connect React frontend with backend
* Axios/fetch
* CORS handling
* Authentication in frontend
## Mini Project
### 🔹 Final Project
## 🚀 MERN Blog / Task Manager App
Features:
* Login / Signup
* Create tasks/blogs
* Edit/delete
* Protected routes
* User dashboard
---
# 🧠 FINAL MASTER PROJECT IDEAS
Pick ONE:
### 🔥 1. Task Manager (like Trello lite)
* drag/drop tasks
* auth
* categories
### 🔥 2. E-commerce API
* products
* cart
* orders
* admin panel APIs
### 🔥 3. Social Media Backend
* posts
* likes
* comments
* follow system
---
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
import { useReducer, useState } from "react";

const initialState = {
  count: 0,
};

function reducer(state, action) {
  switch (action.type) {
    case "increment":
      return {
        count: state.count + 1,
      };

    case "decrement":
      return {
        count: Math.max(0, state.count - 1),
      };

    case "incrementBy":
      return {
        count: state.count + action.payload,
      };

    case "decrementBy":
      return {
        count: Math.max(0, state.count - action.payload),
      };

    case "reset":
      return initialState;

    default:
      return state;
  }
}

export default function App() {
  const [state, dispatch] = useReducer(reducer, initialState);
  const [value, setValue] = useState("");

  const handleAdd = () => {
    const number = Number(value);

    if (!isNaN(number) && number > 0) {
      dispatch({
        type: "incrementBy",
        payload: number,
      });
    }

    setValue("");
  };

  return (
    <div
      style={{
        textAlign: "center",
        marginTop: "50px",
        fontFamily: "Arial",
      }}
    >
      <h1>useReducer Counter App</h1>

      <h2>Count: {state.count}</h2>

      <h3>{state.count % 2 === 0 ? "Even" : "Odd"}</h3>

      <button onClick={() => dispatch({ type: "increment" })}>
        +1
      </button>

      <button onClick={() => dispatch({ type: "decrement" })}>
        -1
      </button>

      <br />
      <br />

      <button
        onClick={() =>
          dispatch({
            type: "incrementBy",
            payload: 10,
          })
        }
      >
        +10
      </button>

      <button
        onClick={() =>
          dispatch({
            type: "decrementBy",
            payload: 10,
          })
        }
      >
        -10
      </button>

      <br />
      <br />

      <button onClick={() => dispatch({ type: "reset" })}>
        Reset
      </button>

      <br />
      <br />

      <input
        type="number"
        placeholder="Enter number"
        value={value}
        onChange={(e) => setValue(e.target.value)}
      />

      <button onClick={handleAdd}>Add</button>
    </div>
  );
}
-- --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

const reducer(state,action);
const [state,dispatch] = uesReducer(reducer,iitialState);

these are 2 important kpi in a term of useReducer hooks 
1st one is a reducer fuction : take (state,action) as a parameter
2nd one is a declaration : take [state,dispatch] = useReducer(reducer,initialState) asa parameter
-- --------------------------------------------------------------------


const reducer=(state,action)=>{}
const [state,dispatch]= useReducer(reducer,initialState);


-- ------------------------------------------------------------------------
import { useReducer } from "react";

//reducer function-------------------------
const reducer = (state, action) => {
  switch (action.type) {
    case "increment":
      return (state = state + 1);

    case "decrement":
      return (state = state - 1);

    case "reset":
      return (state = 0);

    default:
      return state;
  }
};
// end of a reducer function-----------------
// initial state
const initialState = 0;
//end of a setting initial state-------------

// end of a outsiders-------------------------------------------------------------------------
function ReducerCount() {
  const [state, dispatch] = useReducer(reducer, initialState);
  return (
    <>
      <h1>reducerCount</h1>
      <h1>count : {state}</h1>

      <button onClick={() => dispatch({ type: "increment" })}>increment</button>
      <button onClick={() => dispatch({ type: "decrement" })}>decrement</button>
      <button onClick={() => dispatch({ type: "reset" })}>reset</button>
    </>
  );
}

export default ReducerCount;
-----------------------------------------------------------------------------------------------

import { useReducer } from "react";

const initialState = 0;

const reducer = (state, action) => {
  switch (action.type) {
    case "increment":
      return state + action.payload;

    case "decrement":
      return state - action.payload;

    case "reset":
      return initialState;

    default:
      return state;
  }
};

//-------------------------------------------------------------------
function ReducerPayload() {
  const [state, dispatch] = useReducer(reducer, initialState);

  return (
    <>
      <h1>Counter using UseReducer</h1>
      <h2>count : {state}</h2>
      <button onClick={() => dispatch({ type: "increment", payload: 5 })}>
        increment
      </button>
      <button onClick={() => dispatch({ type: "decrement", payload: 5 })}>
        decrement
      </button>
      <button onClick={() => dispatch({ type: "reset" })}>reset</button>
    </>
  );
}
//----------------------------------------------------------------------

export default ReducerPayload;
---------------------------------------------------------------------------------------------


-------------------------------------------------------------------------------------------------------------

🎯 COMPLETE REACT PRACTICE LIST FROM SCRATCH
BEGINNER LEVEL (Concepts: State, Props, Events, Basic Hooks)
Day 1-3: UI Components
#	Project	Key Concepts	Difficulty
1	Counter App	useState, onClick	⭐
2	Character Counter	useState, onChange, maxLength	⭐
3	Auto Focus Input	useRef, useEffect, focus()	⭐
4	Live Clock	useEffect, setInterval, Date()	⭐
5	Stopwatch	useState, useEffect, start/stop/reset	⭐⭐
6	Theme Switcher	useState, className toggle	⭐
Day 4-6: List & Forms
#	Project	Key Concepts	Difficulty
7	Todo List (CRUD)	useState, map, filter, forms	⭐⭐
8	Shopping Cart	useState, add/remove/update	⭐⭐
9	Expense Tracker	useState, forms, list rendering	⭐⭐
10	Product Search	useState, filter, search input	⭐⭐
11	Weather Search App	useState, useEffect, fetch API	⭐⭐
12	Multi-step Form	useState, form validation, conditional	⭐⭐⭐
INTERMEDIATE LEVEL (Concepts: useEffect, Custom Hooks, Context)
Week 2: Custom Hooks
#	Project	Key Concepts	Difficulty
13	Window Size Tracker	custom useWindowSize	⭐⭐
14	Local Storage Sync	custom useLocalStorage	⭐⭐
15	Previous Value Hook	custom usePrevious, useRef	⭐⭐⭐
16	Debounced Search	custom useDebounce, useCallback	⭐⭐⭐
17	Toggle Hook	custom useToggle	⭐⭐
18	Click Outside Hook	custom useClickOutside, useRef	⭐⭐⭐
Week 3: Context API
#	Project	Key Concepts	Difficulty
19	Theme Context	useContext, ThemeProvider	⭐⭐
20	User Authentication	useContext, AuthProvider	⭐⭐⭐
21	Language Switcher	nested Context, i18n	⭐⭐⭐
22	Notification System	useContext, add/remove notifications	⭐⭐⭐
23	Shopping Cart Context	useContext, cart operations	⭐⭐⭐
24	User Preferences	Context + LocalStorage	⭐⭐⭐
Week 4: useReducer
#	Project	Key Concepts	Difficulty
25	Todo with Reducer	useReducer, dispatch actions	⭐⭐⭐
26	Shopping Cart Reducer	useReducer, complex state	⭐⭐⭐
27	Undo/Redo Todo	useReducer with history	⭐⭐⭐⭐
28	Expense Analytics	useReducer, calculations	⭐⭐⭐
29	Form Reducer	useReducer for complex forms	⭐⭐⭐
30	Shopping Cart with Reducer	useReducer + Context	⭐⭐⭐⭐
ADVANCED LEVEL (Concepts: Performance, Optimization, Advanced Patterns)
Week 5: Performance Optimization
#	Project	Key Concepts	Difficulty
31	Memoized Component	React.memo, useMemo	⭐⭐⭐
32	Optimized Search	useMemo, useCallback, debounce	⭐⭐⭐
33	Parent-Child Optimization	React.memo, useCallback, memo	⭐⭐⭐⭐
34	Virtualized List	react-window, useMemo	⭐⭐⭐⭐
35	Lazy Loading Images	React.lazy, Suspense	⭐⭐⭐
36	Performance Dashboard	Profiler, why-did-you-render	⭐⭐⭐⭐
Week 6: Advanced Patterns
#	Project	Key Concepts	Difficulty
37	Compound Tabs	compound components pattern	⭐⭐⭐⭐
38	Accordion	compound components, context	⭐⭐⭐
39	Modal with Portal	ReactDOM.createPortal	⭐⭐⭐
40	Render Props	render props pattern	⭐⭐⭐⭐
41	HOC Authentication	Higher Order Components	⭐⭐⭐
42	Controlled Form	controlled components	⭐⭐⭐
ROUTING & NAVIGATION
Week 7: React Router
#	Project	Key Concepts	Difficulty
43	Basic Blog	Routes, Links, Outlet	⭐⭐
44	Protected Routes	PrivateRoute, Auth	⭐⭐⭐
45	Nested Routes	useParams, Outlet, nested layouts	⭐⭐⭐
46	Product Details	useParams, useNavigate	⭐⭐
47	Search with Query	useSearchParams	⭐⭐⭐
48	Lazy Routes	React.lazy + Suspense	⭐⭐⭐
DATA FETCHING & API
Week 8: API Integration
#	Project	Key Concepts	Difficulty
49	Fetch with useEffect	useEffect, fetch, loading states	⭐⭐
50	CRUD with API	Axios, POST, PUT, DELETE	⭐⭐⭐
51	React Query Basics	useQuery, isLoading	⭐⭐⭐
52	Infinite Scroll	React Query, Intersection Observer	⭐⭐⭐⭐
53	Optimistic Updates	useMutation, optimistic UI	⭐⭐⭐⭐
54	Error Boundary	ErrorBoundary class, fallback	⭐⭐⭐
FULL STACK PROJECTS
Week 9-10: Complete Applications
#	Project	Features
55	E-Commerce Store	Auth, Cart, Checkout, Search, Filter, Payments
56	Task Management	Drag & Drop, Status, Priority, Labels, Search
57	Social Media Dashboard	Posts, Comments, Likes, Real-time, Notifications
58	Expense Manager	Charts, Categories, Reports, Filters, Export
59	Blog Platform	Posts, Tags, Comments, Search, Categories
60	Project Management	Boards, Tasks, Members, Progress, Analytics
TESTING
Week 11: Testing
#	Project	Key Concepts	Difficulty
61	Unit Testing	Jest, React Testing Library	⭐⭐⭐
62	Component Testing	fireEvent, userEvent	⭐⭐⭐
63	API Mocking	MSW, Jest Mock	⭐⭐⭐⭐
64	Integration Testing	testing user flows	⭐⭐⭐⭐
65	E2E Testing	Cypress/Playwright	⭐⭐⭐⭐
MODERN REACT 18+ FEATURES
Week 12: Latest Features
#	Project	Key Concepts	Difficulty
66	Concurrent Features	useTransition, startTransition	⭐⭐⭐⭐
67	Deferred Value	useDeferredValue	⭐⭐⭐⭐
68	Suspense Data Fetching	Suspense, fallback	⭐⭐⭐
69	Server Components	RSC, async components	⭐⭐⭐⭐⭐
70	Actions & Hooks	useFormStatus, useOptimistic	⭐⭐⭐⭐
TYPESCRIPT + REACT
Week 13: TypeScript Integration
#	Project	Key Concepts	Difficulty
71	Typed Props	interface, type, generics	⭐⭐⭐
72	Typed Hooks	useState, useEffect types	⭐⭐⭐
73	Typed Context	Context with TypeScript	⭐⭐⭐⭐
74	Typed API Calls	Axios + TypeScript	⭐⭐⭐⭐
75	Full TS Project	TypeScript + React + React Query	⭐⭐⭐⭐⭐
REDUX (Optional but Good to Know)
Week 14: Redux Toolkit
#	Project	Key Concepts	Difficulty
76	Redux Counter	createSlice, useSelector	⭐⭐⭐
77	Redux Todo	Actions, Reducers, Store	⭐⭐⭐
78	Redux Async	createAsyncThunk	⭐⭐⭐⭐
79	Redux E-Commerce	Complex Redux State	⭐⭐⭐⭐
80	Redux Toolkit Query	RTK Query	⭐⭐⭐⭐
MISCELLANEOUS ESSENTIALS
Week 15: Extra Features
#	Project	Key Concepts	Difficulty
81	File Upload	useRef, FormData, progress	⭐⭐⭐
82	Drag & Drop	onDrag, useRef, reorder	⭐⭐⭐⭐
83	Infinite Scrolling	Intersection Observer	⭐⭐⭐
84	Print Component	window.print(), useRef	⭐⭐
85	Export to CSV/PDF	download, libraries	⭐⭐⭐
86	Star Rating	hover state, conditional	⭐⭐
87	Image Gallery	lightbox, carousel	⭐⭐⭐
88	Pagination	manual, React Query	⭐⭐⭐
89	Search Filters	multiple filters, checkbox	⭐⭐⭐
90	Real-time Updates	WebSocket, Socket.io	⭐⭐⭐⭐
DEPLOYMENT & DEVOPS
Week 16: Deployment
#	Topic	Practice
91	Environment Variables	.env, Vite env
92	Vercel/Netlify	CI/CD deployment
93	Dockerize React	Dockerfile, nginx
94	Error Tracking	Sentry setup
95	Analytics	Google Analytics
--- ----------------------------------------------------------------------------------------------------------------------


db.contacts.insertOne({
first_name:"akshay",
last_name:"kumar",
email:"ak@gamil.com",
phone:"90909090",
address: "123 , ab road , mumbai"
})





WO-O-WG-B-WB-G-WB-B
OGB
OH GOD BOY















